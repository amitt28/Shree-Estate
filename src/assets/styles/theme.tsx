import { createTheme } from '@mui/material/styles';

// Create a theme with Poppins font for all typography
const theme = createTheme({
  typography: {
    fontFamily: 'Poppins, sans-serif', // Sets Poppins as the default font
  },
});

export default theme;
